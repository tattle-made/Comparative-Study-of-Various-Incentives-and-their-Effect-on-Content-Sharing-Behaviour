exports.scheduleReminderEmais = (user) => {
  // iterate through each row
  // for each row look at the 3 relevant fields
  // change their value to SCHEDULE if apt
  // do nothing if not
};

exports.sendReminderEmails = (user) => {
  // iterate through each row
  // for each row look at the 3 relevant fields
  // if any of the value is SCHEDULED
  // send apt emails
};
